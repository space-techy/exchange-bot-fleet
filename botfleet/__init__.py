"""
botfleet — a modular load generator for order-matching engines.

Subpackages:
  core           order generation and the test-plan catalogue (the "what to send").
  runtime        the production bot fleet that sends orders and ships telemetry.
  visualization  offline/live debugging tools that reconstruct and plot the book.
"""
