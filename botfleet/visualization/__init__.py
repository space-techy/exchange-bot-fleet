"""
Debugging / inspection tools.

  books.py     order-book reconstructions (local sim + engine-truth + intent).
  summary.py   per-phase text summary shared by both visualizers.
  plots.py     matplotlib chart helpers.
  offline.py   offline visualizer — local sim, no network.
  live.py      live visualizer — drives the fleet and rebuilds the book from replies.
"""
